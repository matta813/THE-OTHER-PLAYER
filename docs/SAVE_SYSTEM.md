# Save System
JSON saves carry version 1, player position, story stage, trust, raw behaviour events, derived model, and stable-ID facility state. Future schema changes increment the version and migrate forward. Node paths are deliberately excluded. Completed-run profiles will be stored separately for New Game+.
