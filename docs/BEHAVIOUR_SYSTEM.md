# Behaviour System
BehaviourEvent preserves timestamp, type, position, target, context, response time, prior related event, and metadata. BehaviourModel deterministically derives bounded metrics. Raw events remain available for replay, debugging, saves, and future New Game+ profiles.
